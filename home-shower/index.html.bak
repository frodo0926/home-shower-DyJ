<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Home Shower · Hogar Daniel y Jimena</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,400;1,9..144,500&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --paper: #FBF7F4;
      --blush: #E8D8CE;
      --rose: #B98F88;
      --taupe: #7A6258;
      --ink: #3B2F2A;
      --mist: #E8D8CE;
      --khaki: #E8D8CE;
      --olive: #3B2F2A;
      --moss: #7A6258;
      --sage: #B98F88;
      --slate: #B98F88;
      --sky: #E8D8CE;
      --muted: #7A6258;
      --surface: #FFFCFA;
      --shadow: 0 18px 40px rgba(59, 47, 42, 0.10);
      --radius: 20px;
    }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      margin: 0;
      color: var(--ink);
      font-family: Outfit, "Segoe UI", sans-serif;
      background: var(--paper);
      -webkit-font-smoothing: antialiased;
    }
    body::before {
      content: "";
      position: fixed;
      inset: 0;
      background-image: url("images/textura-lino.png");
      background-size: 420px;
      opacity: 0.16;
      pointer-events: none;
      z-index: 0;
      mix-blend-mode: multiply;
    }
    .page { position: relative; z-index: 1; }

    .hero {
      text-align: center;
      padding: 52px 24px 0;
    }
    .eyebrow {
      font-size: 12px;
      letter-spacing: 0.32em;
      text-transform: uppercase;
      font-weight: 600;
      color: var(--moss);
      margin-bottom: 14px;
    }
    .hero h1 {
      font-family: Fraunces, Georgia, serif;
      font-weight: 500;
      font-size: clamp(44px, 8vw, 82px);
      line-height: 0.95;
      margin: 0;
      color: var(--olive);
    }
    .hero h1 em {
      font-style: italic;
      font-weight: 400;
      color: var(--rose);
    }
    .home-name {
      font-family: Fraunces, Georgia, serif;
      font-style: italic;
      font-weight: 400;
      font-size: clamp(22px, 4vw, 34px);
      color: var(--rose);
      margin: 14px 0 0;
    }
    .branch {
      width: min(420px, 78vw);
      height: auto;
      margin: 8px auto 6px;
      display: block;
    }
    .hero .lead {
      max-width: 560px;
      margin: 8px auto 0;
      color: var(--muted);
      font-size: 17px;
      line-height: 1.7;
      font-weight: 300;
    }

    .hero-art-wrap {
      margin-top: 28px;
      overflow: hidden;
    }
    .hero-art {
      width: 100%;
      max-height: 520px;
      object-fit: cover;
      object-position: center 70%;
      display: block;
    }

    .wrap { max-width: 1120px; margin: 0 auto; padding: 0 22px 80px; }

    .intro-card {
      margin: -36px auto 36px;
      max-width: 720px;
      background: color-mix(in srgb, var(--surface) 86%, transparent);
      backdrop-filter: blur(8px);
      border: 1px solid color-mix(in srgb, var(--khaki) 70%, white);
      border-radius: 24px;
      padding: 22px 26px;
      box-shadow: var(--shadow);
      text-align: center;
      color: var(--muted);
      line-height: 1.65;
      font-size: 15.5px;
    }

    .stats {
      display: flex;
      justify-content: center;
      gap: 28px;
      flex-wrap: wrap;
      margin: 8px 0 28px;
    }
    .stat {
      min-width: 140px;
      background: var(--surface);
      border: 1px solid var(--khaki);
      border-radius: 16px;
      padding: 14px 18px;
      text-align: center;
    }
    .stat .num {
      font-family: Fraunces, Georgia, serif;
      font-size: 30px;
      color: var(--olive);
    }
    .stat .lbl {
      font-size: 11px;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--moss);
    }

    .controls {
      display: flex;
      gap: 10px;
      justify-content: center;
      flex-wrap: wrap;
      margin-bottom: 18px;
    }
    .search-box { flex: 1; max-width: 340px; }
    .search-box input,
    .filter-select,
    .ghost-btn {
      width: 100%;
      padding: 12px 16px;
      border-radius: 14px;
      border: 1.5px solid var(--khaki);
      background: var(--surface);
      color: var(--ink);
      font-family: Outfit, sans-serif;
      font-size: 15px;
    }
    .filter-select, .ghost-btn { width: auto; cursor: pointer; }
    .ghost-btn {
      color: var(--olive);
      font-weight: 600;
    }
    .ghost-btn:hover { background: var(--mist); }
    input:focus, select:focus {
      outline: 2px solid var(--sky);
      border-color: var(--slate);
    }

    .chips {
      display: flex;
      justify-content: center;
      gap: 8px;
      flex-wrap: wrap;
      margin-bottom: 28px;
    }
    .chip {
      border: 1px solid var(--khaki);
      background: transparent;
      color: var(--moss);
      border-radius: 999px;
      padding: 8px 14px;
      font-size: 13px;
      font-weight: 500;
      cursor: pointer;
      font-family: Outfit, sans-serif;
    }
    .chip.active {
      background: var(--olive);
      border-color: var(--olive);
      color: var(--paper);
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 22px;
    }

    .scene { perspective: 1400px; }
    .flip {
      position: relative;
      height: 440px;
      transform-style: preserve-3d;
      transition: transform 0.9s cubic-bezier(.19,1,.22,1);
    }
    .flip.is-flipped { transform: rotateY(180deg); }
    .face {
      position: absolute;
      inset: 0;
      backface-visibility: hidden;
      -webkit-backface-visibility: hidden;
      border-radius: var(--radius);
      overflow: hidden;
      box-shadow: var(--shadow);
    }
    .face-front {
      background: var(--surface);
      border: 1px solid var(--khaki);
      padding: 22px 20px 18px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }
    .face-back {
      transform: rotateY(180deg);
      background: linear-gradient(165deg, #FBF7F4 0%, #E8D8CE 100%);
      border: 1px solid var(--rose);
      padding: 20px 18px 14px;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      color: var(--ink);
    }
    .flip.is-flipped .face-back {
      box-shadow: 0 16px 36px rgba(185, 143, 136, 0.28);
    }

    .icon-well {
      width: 64px;
      height: 64px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: color-mix(in srgb, var(--sage) 35%, var(--paper));
      color: var(--olive);
      margin-bottom: 4px;
    }
    .cat {
      align-self: flex-start;
      font-size: 11px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--moss);
      background: color-mix(in srgb, var(--khaki) 45%, white);
      padding: 4px 10px;
      border-radius: 999px;
      font-weight: 600;
    }
    .card-title {
      font-family: Fraunces, Georgia, serif;
      font-size: 24px;
      font-weight: 500;
      line-height: 1.15;
      margin: 0;
    }
    .card-detail {
      color: var(--muted);
      font-size: 14px;
      line-height: 1.45;
      min-height: 40px;
    }
    .spacer { flex: 1; }
    .choose-btn {
      width: 100%;
      border: none;
      background: var(--olive);
      color: var(--paper);
      font-family: Outfit, sans-serif;
      font-weight: 600;
      font-size: 14px;
      padding: 12px 14px;
      border-radius: 12px;
      cursor: pointer;
    }
    .choose-btn:hover { background: #2a211e; }

    .seal {
      width: 86px;
      height: 86px;
      object-fit: contain;
      margin-bottom: 4px;
      background: transparent;
    }
    .thanks-name {
      font-family: Fraunces, Georgia, serif;
      font-size: 26px;
      font-style: italic;
      font-weight: 500;
      margin: 2px 0 10px;
      line-height: 1.1;
      color: var(--ink);
    }
    .verse {
      font-family: Fraunces, Georgia, serif;
      font-style: italic;
      font-size: 15px;
      line-height: 1.4;
      margin: 0;
      color: var(--taupe);
    }
    .cite {
      margin-top: 6px;
      font-size: 12px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--rose);
      font-weight: 600;
    }
    .thanks-msg {
      margin: 12px 0 0;
      font-size: 13.5px;
      line-height: 1.45;
      color: var(--ink);
      font-weight: 400;
    }
    .release-btn {
      margin-top: auto;
      background: transparent;
      border: 1px solid color-mix(in srgb, var(--olive) 35%, white);
      color: var(--olive);
      border-radius: 999px;
      padding: 8px 12px;
      font-size: 12px;
      cursor: pointer;
      font-family: Outfit, sans-serif;
    }
    .release-btn:hover { background: color-mix(in srgb, white 50%, transparent); }

    .empty, .loading {
      grid-column: 1 / -1;
      text-align: center;
      padding: 50px 16px;
      font-family: Fraunces, Georgia, serif;
      color: var(--muted);
      font-size: 20px;
    }

    footer {
      text-align: center;
      color: var(--moss);
      font-size: 13px;
      padding: 40px 16px 24px;
    }

    .overlay {
      position: fixed;
      inset: 0;
      background: rgba(59, 47, 42, 0.42);
      display: none;
      align-items: center;
      justify-content: center;
      z-index: 50;
      padding: 20px;
    }
    .overlay.open { display: flex; }
    .modal {
      width: min(420px, 100%);
      background: var(--surface);
      border-radius: 22px;
      padding: 28px 24px 22px;
      box-shadow: 0 24px 60px rgba(0,0,0,.18);
      text-align: center;
      border: 1px solid var(--khaki);
    }
    .modal img { width: 64px; margin-bottom: 6px; }
    .modal h3 {
      font-family: Fraunces, Georgia, serif;
      font-weight: 500;
      font-size: 26px;
      margin: 0 0 6px;
    }
    .modal .item { color: var(--slate); font-weight: 600; margin: 0 0 16px; }
    .modal p.hint { color: var(--muted); font-size: 14px; margin: 0 0 14px; }
    .modal input {
      width: 100%;
      padding: 12px 14px;
      border-radius: 12px;
      border: 1.5px solid var(--khaki);
      font-family: Outfit, sans-serif;
      font-size: 15px;
      margin-bottom: 16px;
    }
    .modal-actions { display: flex; gap: 10px; justify-content: flex-end; }
    .btn-cancel, .btn-ok {
      padding: 10px 16px;
      border-radius: 12px;
      font-weight: 600;
      font-family: Outfit, sans-serif;
      cursor: pointer;
    }
    .btn-cancel {
      border: 1.5px solid var(--khaki);
      background: transparent;
      color: var(--muted);
    }
    .btn-ok {
      border: none;
      background: var(--olive);
      color: var(--paper);
    }

    .toast {
      position: fixed;
      left: 50%;
      bottom: 24px;
      transform: translateX(-50%) translateY(20px);
      background: var(--olive);
      color: var(--paper);
      padding: 12px 18px;
      border-radius: 999px;
      opacity: 0;
      pointer-events: none;
      transition: .35s ease;
      z-index: 60;
      font-size: 14px;
    }
    .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

    @media (prefers-reduced-motion: reduce) {
      .flip { transition: none; }
    }
    @media (max-width: 640px) {
      .flip { height: 460px; }
      .intro-card { margin-top: 18px; }
    }
  </style>
</head>
<body>
  <div class="page">
    <header class="hero">
      <div class="eyebrow">Una casa se llena de amor</div>
      <h1>Home Shower</h1>
      <p class="home-name">Hogar Daniel y Jimena</p>
      <img class="branch" src="images/rama-oliva.png" alt="" />
      <p class="lead">
        Gracias por venir a bendecir esta casa. Elige un detalle para el hogar:
        la tarjeta se voltea con un versículo y nuestro agradecimiento, escrito para ti.
      </p>
    </header>
    <div class="hero-art-wrap">
      <img class="hero-art" src="images/hero-hogar.png" alt="Daniel y Jimena en la puerta de su hogar, ilustración en acuarela" />
    </div>

    <main class="wrap">
      <div class="intro-card">
        Cada regalo es una semilla en este nido. Si eliges uno, la tarjeta gira
        y queda a tu nombre, con un versículo. Si cambias de parecer, puedes
        devolverla y vuelve a su cara original — para que otro ser querido la tome.
      </div>

      <div class="stats" id="statsBar"></div>

      <div class="controls">
        <div class="search-box">
          <input id="searchInput" type="search" placeholder="Buscar un rincón del hogar…" />
        </div>
        <select class="filter-select" id="filterSelect">
          <option value="todos">Todos</option>
          <option value="disponibles">Disponibles</option>
          <option value="elegidos">Ya elegidos</option>
        </select>
        <button class="ghost-btn" id="refreshBtn" type="button">↻ Actualizar</button>
      </div>

      <div class="chips" id="chips"></div>
      <div class="grid" id="grid"></div>

      <footer>Hogar Daniel y Jimena · “Yo y mi casa serviremos a Jehová.” Josué 24:15</footer>
    </main>
  </div>

  <div class="overlay" id="overlay">
    <div class="modal" role="dialog" aria-modal="true">
      <img src="images/sello-hogar.png" alt="" />
      <h3>¿Quién regala este detalle?</h3>
      <p class="item" id="modalItem"></p>
      <p class="hint">Escribe tu nombre para voltear la tarjeta y dejar el versículo a tu nombre.</p>
      <input id="modalName" type="text" maxlength="60" placeholder="Tu nombre completo" autocomplete="name" />
      <div class="modal-actions">
        <button class="btn-cancel" id="modalCancel" type="button">Cancelar</button>
        <button class="btn-ok" id="modalOk" type="button">Elegir y voltear</button>
      </div>
    </div>
  </div>
  <div class="toast" id="toast"></div>

  <script>
    /* ========= PERSONALIZA AQUÍ ========= */
    const CONFIG = {
      hosts: "Hogar Daniel y Jimena",
      storageKey: "home-shower-regalos-v1",
      /* Pega aquí la URL /exec de Google Apps Script (ver README.md) */
      apiUrl: ""
    };

    const ICONS = {
      bed: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M4 18V8.5A1.5 1.5 0 0 1 5.5 7h7A1.5 1.5 0 0 1 14 8.5V12h10v6"/><path d="M3 18h24M6 18v4M24 18v4"/></svg>',
      towel: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M8 6h10a4 4 0 0 1 4 4v14H12a4 4 0 0 1-4-4V6z"/><path d="M8 10h8"/></svg>',
      pot: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M7 13h16l-1.2 10.5a2 2 0 0 1-2 1.7H10.2a2 2 0 0 1-2-1.7L7 13z"/><path d="M5 13h20M12 8c0-2 6-2 6 0"/></svg>',
      plate: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><circle cx="15" cy="15" r="10"/><circle cx="15" cy="15" r="5"/></svg>',
      coffee: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M7 10h14v8a5 5 0 0 1-5 5h-4a5 5 0 0 1-5-5v-8z"/><path d="M21 12h3a3 3 0 0 1 0 6h-3"/><path d="M11 5v3M16 5v3"/></svg>',
      cutlery: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M9 5v20M7 5c0 3 4 3 4 6v2H7"/><path d="M20 5v8c0 2-1.5 3-3 3v9"/><path d="M17 5v8"/></svg>',
      linen: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M6 8h18v16H6z"/><path d="M6 12h18M10 8v16"/></svg>',
      lamp: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M10 12h10l-1.5 8h-7L10 12z"/><path d="M15 4v8M11 28h8M15 20v8"/></svg>',
      cushion: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="7" y="7" width="16" height="16" rx="4"/><path d="M11 11h8v8h-8z"/></svg>',
      glass: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M9 5h12l-1.5 10H10.5L9 5z"/><path d="M15 15v8M11 25h8"/></svg>',
      plant: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M15 18c0-8 6-10 6-10s0 8-6 10c-6-2-6-10-6-10s6 2 6 10z"/><path d="M11 26h8l-1  -4h-6l-1 4z"/></svg>',
      art: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="6" y="7" width="18" height="16" rx="1"/><path d="M6 19l5-5 4 4 3-3 6 6"/></svg>',
      knife: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M7 22l14-14 2 2-8 16c-3 1-6-1-8-4z"/></svg>',
      candle: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M10 14h10v12H10z"/><path d="M15 14c0-4 3-5 3-8"/></svg>',
      rug: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><ellipse cx="15" cy="16" rx="11" ry="7"/><path d="M7 16h16"/></svg>',
      tea: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M8 12h13v9a4 4 0 0 1-4 4h-5a4 4 0 0 1-4-4v-9z"/><path d="M21 14h2.5a2.5 2.5 0 1 1 0 5H21"/></svg>',
      basket: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M6 14h18l-2 10H8L6 14z"/><path d="M10 14V9a5 5 0 0 1 10 0v5"/></svg>',
      gift: '<svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="6" y="12" width="18" height="13" rx="1"/><path d="M15 12v13M6 17h18"/><path d="M15 12c-3-5-8-3-5 0h5c3-3-2-5-5 0"/></svg>'
    };

    const GIFTS = [
      { id: "sabanas", articulo: "Juego de sábanas", categoria: "Cama y baño", detalle: "Algodón suave, cama doble", icon: "bed",
        versiculo: "Con sabiduría se edificará la casa, y con prudencia se afirmará.", cita: "Proverbios 24:3" },
      { id: "toallas", articulo: "Set de toallas", categoria: "Cama y baño", detalle: "Baño y manos, algodón grueso", icon: "towel",
        versiculo: "Jehová te bendiga y te guarde; Jehová haga resplandecer su rostro sobre ti.", cita: "Números 6:24-25" },
      { id: "bateria", articulo: "Batería de cocina", categoria: "Cocina", detalle: "Ollas y sartenes para el día a día", icon: "pot",
        versiculo: "Si Jehová no edificare la casa, en vano trabajan los que la edifican.", cita: "Salmo 127:1" },
      { id: "vajilla", articulo: "Vajilla para seis", categoria: "Mesa", detalle: "Platos, tazas y ensaladera", icon: "plate",
        versiculo: "Hospedaos los unos a los otros sin murmuraciones.", cita: "1 Pedro 4:9" },
      { id: "cafetera", articulo: "Cafetera", categoria: "Cocina", detalle: "Para las mañanas lentas de esta casa", icon: "coffee",
        versiculo: "Este es el día que hizo Jehová; nos gozaremos y alegraremos en él.", cita: "Salmo 118:24" },
      { id: "cubiertos", articulo: "Juego de cubiertos", categoria: "Mesa", detalle: "Acero, servicio completo", icon: "cutlery",
        versiculo: "Y sobre todas estas cosas vestíos de amor, que es el vínculo perfecto.", cita: "Colosenses 3:14" },
      { id: "linos", articulo: "Manteles de lino", categoria: "Mesa", detalle: "Mantel y servilletas para compartir", icon: "linen",
        versiculo: "No os olvidéis de la hospitalidad, porque por ella algunos, sin saberlo, hospedaron ángeles.", cita: "Hebreos 13:2" },
      { id: "lampara", articulo: "Lámpara de mesa", categoria: "Sala", detalle: "Luz cálida para la noche", icon: "lamp",
        versiculo: "Lámpara es a mis pies tu palabra, y lumbrera a mi camino.", cita: "Salmo 119:105" },
      { id: "cojines", articulo: "Cojines para la sala", categoria: "Sala", detalle: "Texturas de lino y lana", icon: "cushion",
        versiculo: "Y mi pueblo habitará en morada de paz, en habitaciones seguras.", cita: "Isaías 32:18" },
      { id: "cristaleria", articulo: "Vasos y copas", categoria: "Mesa", detalle: "Agua, vino y celebraciones", icon: "glass",
        versiculo: "Alabad a Jehová, porque él es bueno; porque para siempre es su misericordia.", cita: "1 Crónicas 16:34" },
      { id: "planta", articulo: "Planta de interior", categoria: "Sala", detalle: "Maceta de barro y verdor", icon: "plant",
        versiculo: "Plantados en la casa de Jehová, florecerán en los atrios de nuestro Dios.", cita: "Salmo 92:13" },
      { id: "cuadro", articulo: "Lámina para la sala", categoria: "Sala", detalle: "Arte sereno, marco de madera", icon: "art",
        versiculo: "Sea agradable sobre nosotros la luz de Jehová nuestro Dios, y confirma la obra de nuestras manos.", cita: "Salmo 90:17" },
      { id: "cuchillos", articulo: "Set de cuchillos", categoria: "Cocina", detalle: "Buen filo para cocinar en casa", icon: "knife",
        versiculo: "Y todo lo que hagáis, hacedlo de corazón, como para el Señor.", cita: "Colosenses 3:23" },
      { id: "velas", articulo: "Velas y aroma", categoria: "Detalles", detalle: "Cera suave, notas de olivo y lino", icon: "candle",
        versiculo: "Mejor es la comida de legumbres donde hay amor, que de buey engordado donde hay odio.", cita: "Proverbios 15:17" },
      { id: "alfombra", articulo: "Alfombra pequeña", categoria: "Sala", detalle: "Para el recibidor o el pie de cama", icon: "rug",
        versiculo: "En paz me acostaré, y asimismo dormiré, porque solo tú, Jehová, me haces vivir confiado.", cita: "Salmo 4:8" },
      { id: "te", articulo: "Set de té y café", categoria: "Cocina", detalle: "Tetera, tazas y bandeja", icon: "tea",
        versiculo: "Gustad y ved que es bueno Jehová; dichoso el hombre que confía en él.", cita: "Salmo 34:8" },
      { id: "canasta", articulo: "Canasta para el pan", categoria: "Mesa", detalle: "Mimbre y lienzo para la mesa", icon: "basket",
        versiculo: "El pan nuestro de cada día, dánoslo hoy.", cita: "Mateo 6:11" },
      { id: "bono-cocina", articulo: "Bono para la cocina", categoria: "Aportes", detalle: "Un aporte libre para utensilios", icon: "gift",
        versiculo: "Cada uno dé como propuso en su corazón: no con tristeza, ni por necesidad, porque Dios ama al dador alegre.", cita: "2 Corintios 9:7" },
      { id: "bono-nido", articulo: "Bono para el nido", categoria: "Aportes", detalle: "Para textiles, luz o lo que falte", icon: "gift",
        versiculo: "Pero yo y mi casa serviremos a Jehová.", cita: "Josué 24:15" }
    ];

    let regalos = [];
    let pendingId = null;
    let categoria = "Todas";
    const mine = new Set(JSON.parse(sessionStorage.getItem("home-shower-mine") || "[]"));
    const useApi = () => !!(CONFIG.apiUrl && CONFIG.apiUrl.trim());

    function hydrate(row) {
      const base = GIFTS.find(g => g.id === row.id) || {};
      return {
        id: String(row.id || base.id || ""),
        articulo: row.articulo || base.articulo || "",
        categoria: row.categoria || base.categoria || "",
        detalle: row.detalle || base.detalle || "",
        versiculo: row.versiculo || base.versiculo || "",
        cita: row.cita || base.cita || "",
        icon: base.icon || row.icon || "gift",
        reservadoPor: row.reservadoPor ? String(row.reservadoPor).trim() : ""
      };
    }

    function loadLocal() {
      const saved = localStorage.getItem(CONFIG.storageKey);
      if (saved) {
        try {
          const map = JSON.parse(saved);
          regalos = GIFTS.map(g => ({ ...g, reservadoPor: map[g.id] || "" }));
          return;
        } catch (e) { /* ignore */ }
      }
      regalos = GIFTS.map(g => ({ ...g, reservadoPor: "" }));
    }

    function persistLocal() {
      const map = {};
      regalos.forEach(g => { if (g.reservadoPor) map[g.id] = g.reservadoPor; });
      localStorage.setItem(CONFIG.storageKey, JSON.stringify(map));
      sessionStorage.setItem("home-shower-mine", JSON.stringify([...mine]));
    }

    async function apiGet(params) {
      const url = new URL(CONFIG.apiUrl);
      Object.entries(params || {}).forEach(([k, v]) => url.searchParams.set(k, v));
      const res = await fetch(url.toString(), { method: "GET" });
      return res.json();
    }

    async function load() {
      if (!useApi()) {
        loadLocal();
        return;
      }
      try {
        const data = await apiGet();
        const list = Array.isArray(data.regalos) ? data.regalos : [];
        regalos = list.length
          ? list.map(hydrate).filter(g => g.id)
          : GIFTS.map(g => ({ ...g, reservadoPor: "" }));
      } catch (err) {
        console.error(err);
        document.getElementById("grid").innerHTML =
          '<div class="loading">No pudimos conectar la lista. Revisa la URL de Apps Script.</div>';
        throw err;
      }
    }

    function escapeHtml(str) {
      const d = document.createElement("div");
      d.innerText = str;
      return d.innerHTML;
    }

    function renderStats() {
      const total = regalos.length;
      const taken = regalos.filter(g => g.reservadoPor).length;
      document.getElementById("statsBar").innerHTML = `
        <div class="stat"><div class="num">${taken}/${total}</div><div class="lbl">Detalles elegidos</div></div>
        <div class="stat"><div class="num">${total - taken}</div><div class="lbl">Aún disponibles</div></div>
      `;
    }

    function renderChips() {
      const source = regalos.length ? regalos : GIFTS;
      const cats = ["Todas", ...new Set(source.map(g => g.categoria))];
      document.getElementById("chips").innerHTML = cats.map(c =>
        `<button class="chip ${c === categoria ? "active" : ""}" data-cat="${c}">${c}</button>`
      ).join("");
      document.querySelectorAll(".chip").forEach(btn => {
        btn.addEventListener("click", () => {
          categoria = btn.dataset.cat;
          render();
        });
      });
    }

    function render() {
      renderStats();
      renderChips();
      const q = document.getElementById("searchInput").value.trim().toLowerCase();
      const f = document.getElementById("filterSelect").value;
      const box = document.getElementById("grid");
      let html = "";
      let shown = 0;

      regalos.forEach(item => {
        const taken = !!item.reservadoPor;
        if (categoria !== "Todas" && item.categoria !== categoria) return;
        if (q && !item.articulo.toLowerCase().includes(q) && !item.detalle.toLowerCase().includes(q)) return;
        if (f === "disponibles" && taken) return;
        if (f === "elegidos" && !taken) return;
        shown++;
        html += cardHtml(item, taken);
      });

      box.innerHTML = shown
        ? html
        : `<div class="empty">No hay regalos con esa búsqueda en esta casa.</div>`;

      box.querySelectorAll("[data-choose]").forEach(btn => {
        btn.addEventListener("click", () => openModal(btn.dataset.choose));
      });
      box.querySelectorAll("[data-release]").forEach(btn => {
        btn.addEventListener("click", () => release(btn.dataset.release));
      });
    }

    function cardHtml(item, taken) {
      const name = escapeHtml(item.reservadoPor || "");
      return `
        <div class="scene">
          <article class="flip ${taken ? "is-flipped" : ""}" data-card="${item.id}">
            <div class="face face-front">
              <span class="cat">${item.categoria}</span>
              <div class="icon-well">${ICONS[item.icon] || ICONS.gift}</div>
              <h3 class="card-title">${escapeHtml(item.articulo)}</h3>
              <p class="card-detail">${escapeHtml(item.detalle)}</p>
              <div class="spacer"></div>
              <button class="choose-btn" data-choose="${item.id}" type="button">Elegir este detalle</button>
            </div>
            <div class="face face-back">
              <img class="seal" src="images/sello-hogar.png" alt="" />
              <div class="thanks-name">${name}</div>
              <p class="verse">“${escapeHtml(item.versiculo)}”</p>
              <div class="cite">${escapeHtml(item.cita)}</div>
              <p class="thanks-msg">Gracias ${name} por bendecir nuestro hogar con el ${escapeHtml(item.articulo)}</p>
              <div class="spacer"></div>
              <button class="release-btn" data-release="${item.id}" type="button">Cambiar de parecer · volver al frente</button>
            </div>
          </article>
        </div>`;
    }

    function openModal(id) {
      const item = regalos.find(g => g.id === id);
      if (!item || item.reservadoPor) return;
      pendingId = id;
      document.getElementById("modalItem").textContent = item.articulo;
      document.getElementById("modalName").value = "";
      document.getElementById("overlay").classList.add("open");
      document.getElementById("modalName").focus();
    }
    function closeModal() {
      document.getElementById("overlay").classList.remove("open");
      pendingId = null;
    }

    async function confirmChoose() {
      const name = document.getElementById("modalName").value.trim();
      if (!name || !pendingId) {
        document.getElementById("modalName").focus();
        return;
      }
      const item = regalos.find(g => g.id === pendingId);
      if (!item || item.reservadoPor) {
        closeModal();
        await load();
        render();
        toast("Ese detalle ya fue tomado. La lista se actualizó.");
        return;
      }

      const confirmBtn = document.getElementById("modalOk");
      confirmBtn.disabled = true;
      confirmBtn.textContent = "Reservando…";

      try {
        if (useApi()) {
          const result = await apiGet({ action: "reserve", id: item.id, nombre: name });
          if (!result.success) {
            document.getElementById("overlay").classList.add("open");
            await load();
            render();
            toast("Ese cupo ya fue reservado por alguien más. Se actualizó la lista.");
            return;
          }
        } else {
          item.reservadoPor = name;
          persistLocal();
        }
        mine.add(item.id);
        sessionStorage.setItem("home-shower-mine", JSON.stringify([...mine]));
        const id = item.id;
        closeModal();
        await load();
        render();
        requestAnimationFrame(() => {
          const el = document.querySelector(`[data-card="${id}"]`);
          if (el) {
            el.classList.remove("is-flipped");
            void el.offsetWidth;
            el.classList.add("is-flipped");
          }
        });
        toast("La tarjeta se volteó con tu nombre y un versículo.");
      } catch (err) {
        console.error(err);
        toast("Hubo un problema guardando tu reserva. Intenta de nuevo.");
      } finally {
        confirmBtn.disabled = false;
        confirmBtn.textContent = "Elegir y voltear";
      }
    }

    async function release(id) {
      const item = regalos.find(g => g.id === id);
      if (!item || !item.reservadoPor) return;
      const ok = confirm(`¿Dejar disponible de nuevo “${item.articulo}”, elegido por ${item.reservadoPor}?`);
      if (!ok) return;
      try {
        if (useApi()) {
          const result = await apiGet({ action: "release", id });
          if (!result.success) {
            toast("No se pudo liberar. Actualiza e intenta de nuevo.");
            await load();
            render();
            return;
          }
        } else {
          item.reservadoPor = "";
          persistLocal();
        }
        mine.delete(id);
        sessionStorage.setItem("home-shower-mine", JSON.stringify([...mine]));
        await load();
        render();
        toast("La tarjeta volvió a su cara original.");
      } catch (err) {
        console.error(err);
        toast("No se pudo conectar para liberar el regalo.");
      }
    }

    function toast(msg) {
      const t = document.getElementById("toast");
      t.textContent = msg;
      t.classList.add("show");
      setTimeout(() => t.classList.remove("show"), 2600);
    }

    document.getElementById("modalCancel").addEventListener("click", closeModal);
    document.getElementById("modalOk").addEventListener("click", confirmChoose);
    document.getElementById("overlay").addEventListener("click", e => {
      if (e.target.id === "overlay") closeModal();
    });
    document.getElementById("modalName").addEventListener("keydown", e => {
      if (e.key === "Enter") confirmChoose();
    });
    document.getElementById("searchInput").addEventListener("input", render);
    document.getElementById("filterSelect").addEventListener("change", render);
    document.getElementById("refreshBtn").addEventListener("click", () => { load(); render(); toast("Lista actualizada"); });

    load();
    render();
  </script>
</body>
</html>
